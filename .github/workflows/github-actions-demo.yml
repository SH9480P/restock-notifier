name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Check-Chrome-Version:
    runs-on: ubuntu-24.04
    steps:
      - run: echo ${{vars.EMAIL_ADDRESS}}
